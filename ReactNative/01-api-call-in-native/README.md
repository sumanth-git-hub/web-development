# React native Call api with flat list and added authentication using clerk

- created the app with the user authentication using the clerk
- when the user is new or not signed in they can only see the login or register screen
- after the user done with the sign up or sign in they land on the product screen
- for understanding api call and rendering in the app 
- call api and handle the error
- render the items in flatList
- add the loading state using activity indicator
- made the each product clickable and added the dynamic routing
- each product goes through individual screen with the detailed information
- profile screen contains the user info and logout option 
- created the modal component to showcase the user details that fetched from clerk dashboard